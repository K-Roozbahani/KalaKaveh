# Byte-compiled / optimized / DLL files
__pycache__/
*.pyc
*.pyo
*.pyd

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
# Usually these files are written to the root, but they can also be
# written to the build/ directory.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests out/
coverage/
media_root/
static_root/

# Translations
*.mo
*.pot

# Django stuff:
# Compile the cache files here. Uncomment if you are caching.
# *.mo *.po

# Django template cache files
# Usually in STATIC_ROOT or MEDIA_ROOT, so potentially ignored by other rules.
# But explicitly add them here in case:
# cache/
# *.mo *.po

# Virtual environment
venv/
env/
ENV/
env.bak/
venv.bak/

# IDE / Editor specific files
.idea/
.vscode/
*.sublime-project
*.sublime-workspace
nbproject/
*.pydevproject
.project
.pydev_project.xml
.settings/

# OS specific files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Database files (for local development, e.g. SQLite)
db.sqlite3
*.sqlite3
db.sqlite
*.db

# Static and Media files (if you collect them in a dedicated folder)
# This depends on your STATIC_ROOT and MEDIA_ROOT settings.
# If they are in the root of your project, you might need to add them.
# Example:
# /static/
# /media/

# Log files
*.log
logs/

# Secrets and sensitive info (if you accidentally commit them)
# Make sure to properly manage secrets, e.g., using environment variables or secret management tools.
# Example:
# .env
# secrets.yaml

# Docker specific files
docker-compose.yml
docker-compose.*.yml
docker/
.dockerignore

# Python-dotenv
*.env
.venv

# Miscellaneous
*.bak
*~